<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false" scan="true" scanPeriod="1 seconds">

    <contextName>logback</contextName>
    <property name="log.path" value="${LOG_HOME:-/data/applogs}/xxl-job/xxl-job-executor-sample-springboot.log" />
    
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}.%d{yyyy-MM-dd}.log</fileNamePattern>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <!-- 文件日志保持UTF-8编码 -->
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- XXL-JOB 日志 Appender：将 log.info() 等日志自动转发到 XXL-JOB 执行日志 -->
    <appender name="xxlJobLog" class="com.xxl.job.executor.config.XxlJobLogAppender"/>

    <!-- 项目业务日志级别 -->
    <logger name="com.xxl.job.executor" level="info">
        <appender-ref ref="xxlJobLog"/>
    </logger>
    
    <!-- MyBatis-Plus 日志级别 - 只显示错误 -->
    <logger name="com.baomidou.mybatisplus" level="error"/>
    
    <!-- MyBatis 日志级别 - 只显示错误 -->
    <logger name="org.apache.ibatis" level="error"/>
    
    <!-- Spring 框架日志级别 - 只显示警告和错误 -->
    <logger name="org.springframework" level="warn"/>
    
    <!-- Hibernate Validator 日志级别 - 只显示错误 -->
    <logger name="org.hibernate.validator" level="error"/>
    
    <!-- HikariCP 连接池日志级别 - 只显示警告 -->
    <logger name="com.zaxxer.hikari" level="warn"/>
    
    <!-- OkHttp 日志级别 - 只显示警告 -->
    <logger name="okhttp3" level="warn"/>

    <!-- 根日志级别 -->
    <root level="info">
        <appender-ref ref="console"/>
        <!--<appender-ref ref="file"/>-->
    </root>

</configuration>